@model IEnumerable<WebMantenedor.Models.Precios>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Agregar")
</p>
<table class="table">
    <tr>
        @*<th>
            @Html.DisplayNameFor(model => model.PRE_id_pre)
        </th>*@
        <th>
            @Html.DisplayNameFor(model => model.PRE_id_reg)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PRE_id_pes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PRE_pre_pre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model)
{
    <tr>
        @*<td>
            @Html.DisplayFor(modelItem => item.PRE_id_pre)
        </td>*@
        <td>

            @Html.DisplayFor(modelItem => item.PRE_id_reg)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PRE_id_pes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PRE_pre_pre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) 
            @*@Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })*@
        </td>
    </tr>
}

</table>
