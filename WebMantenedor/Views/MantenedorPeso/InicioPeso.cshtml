@model IEnumerable<WebMantenedor.Models.Peso>

@{
    ViewBag.Title = "Inicio - Peso";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>IncioPeso</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PES_id_pes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PES_nom_pes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PES_min_pes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PES_max_pes)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PES_id_pes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PES_nom_pes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PES_min_pes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PES_max_pes)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
